int addTwo(int w, int x)
{
    return (w + x);
}

int subtractTwo(int x, int y)
{
    return (x - y);
}

int multTwo(int x, int y)
{
    return (x * y);
}

float divTwo(float x, float y)
{
    return (x / y);
}